# User help and AI navigation

## Scope

Add one English Markdown user guide under `docs/help/`, shared by people and
AI assistants. Route README and the documentation map to it; generate root
llms.txt from the guide's topic index. Add a dependency-free navigation check
and CI job. Include links to the exact release tag's help in future release
notes. No application runtime behavior changes.

Consulted `AGENTS.md`, `REPOSITORY_RULES.md`, `docs/agents/verification.md`, and
the existing architecture ownership model. Work started from `origin/main`
commit `e0c6572d`; feature instructions were checked against that revision.

## Content review evidence

| Help topic | Implementation consulted |
| --- | --- |
| Login and recovery | `apps/desktop/src/components/auth.tsx`, `SessionVerificationGate.tsx` |
| Navigation and search scope | `apps/desktop/src/components/Shell.tsx`, `panes.tsx`, `mediaLists.tsx` |
| Room creation, invitations, DM | `apps/desktop/src/components/dialogs.tsx`, `RoomInfoPanel.tsx`, `apps/desktop/src/App.tsx` |
| Sending, editing, threads | `apps/desktop/src/components/composer.tsx`, `KeyboardSettingsPanel.tsx`, `apps/desktop/src/domain/contextMenus.ts` |
| Search indexing controls | `apps/desktop/src/components/user-settings/SearchHistorySection.tsx` |
| Key files and recovery entry | `apps/desktop/src/components/user-settings/SecuritySection.tsx`, `UserSettingsPanel.tsx` |
| UI labels | `apps/desktop/src/i18n/messages.ts` |
| Versioned help links | `scripts/desktop-release-version.mjs`, `.github/workflows/release-desktop.yml` |

Reviewed the guide as a reader looking for sign-in, a direct message, missing
search results, and encryption recovery. Corrected labels and operation order
against the source (Accept invite, Edit, Send, file chooser before passphrase).
The DM dialog takes a full Matrix ID; the guide does not promise user search
there. Proposed chat export is explicitly not documented as a shipped action.
Existing trust-model help now links to the index and avoids prescribing a QR
flow without establishing that it is offered by the current app.

## Validation

- `node --test scripts/user-help.test.mjs`: 8 tests passed, including negative
  cases for stale output, orphan/missing/duplicate pages, missing links and
  anchors, missing entry/back links, and release-tag URL generation.
- `node scripts/user-help.mjs --check`: 8 help pages and local navigation passed.
- `node scripts/check-agents-docs.mjs`: passed.
- Workflow YAML parsed and the changed release shell step passed `bash -n`.
- Generated a synthetic release-notes preface and checked that it retains the
  Windows trial notice alongside tagged help links.
- Staged secret scan and `git diff --cached --check`: passed before commit.

No application builds, product suites, homeserver sessions, or native GUI QA
were run for these documentation/tooling changes. External HTTP availability
and semantic correctness of prose are not claims of the link checker. The
release workflow was inspected, not executed; no release was published.
