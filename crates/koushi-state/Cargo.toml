[package]
name = "koushi-state"
version = "0.1.0"
edition = "2024"
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
unicode-normalization = "0.1"
zeroize = "1"

[dev-dependencies]
serde_json = "1"
