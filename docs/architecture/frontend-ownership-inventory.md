# Frontend Semantic Ownership Inventory (#552)

Status: final #552 acceptance inventory, amended by the separately reviewed #759 transport contract. Every listed long-lived owner is Rust product/resource authority, renderer-specific presentation/intent, a transport projection/cache, an adapter resource, or an intentional bounded test mirror.

Pinned epic contract: [`evidence/issue-552-contract.json`](evidence/issue-552-contract.json), SHA-256 `0371538cb18ab90b399fbd8114ec0678603ef3d24797e3f70d182898910c268f`, from <https://github.com/shinaoka/koushi-matrix/issues/552>.

## Classification key

- **Rust product/domain** — durable Matrix/product semantics or backend resource lifetime. React renders a projection and sends typed intent.
- **Renderer presentation** — DOM geometry, focus, transient overlays, immediate feedback, or view-local scheduling. Keep in React.
- **Transport/projection cache** — ordered copy of Rust snapshot/events, including gap/stale detection. Keep as a cache; it is not another semantic owner.
- **Test-backend mirror** — Browser Fake/harness behavior used to test the Rust contract. It is not production authority, but must stay equivalent and bounded.

Final decisions are **keep with renderer/transport reason**, **deleted/migrated**, or **intentional test mirror**. No owner remains merely “investigate.” A long-lived ref is not a migration target by size alone.

## Inventory

| Site / current owner | Lifetime and disappearance | Classification / authority | Settlement and duplicate semantics | Decision |
| --- | --- | --- | --- | --- |
| `domain/appStore.ts::applyAppStoreDeltas` | Page lifetime; reset on account/session snapshot clear | **Transport/projection cache**. Rust `StateDelta.generation` and initial/resync snapshots are authoritative. | Drops duplicate/stale generations; a forward gap atomically requests versioned state resync plus timeline reset/replay. A command generation ahead of the store requests one monotone settlement resync; neither path invents domain transitions. | **Keep.** Already-correct #111 projection cache on the #759 single state-update lane. |
| `domain/timelineStore.ts::TimelineStoreState`, `applyInitialItems`, `applyItemsUpdated` | Page lifetime, bounded inactive keys; keys clear on resync/account reset | **Transport/projection cache** for Rust `CoreEvent::Timeline`. Derived ID/timestamp indices are renderer acceleration only. | Actor/generation/batch fences reject stale diffs; pagination/gap fields copy Rust states. | **Keep.** Do not move maps/indices into Rust or reimplement the transport. |
| `backend/desktopApi.ts::DesktopApi`, link/media, attention, window/dialog and event ports, concrete browser/Tauri adapters, composition modules, and direct `@tauri-apps/*` imports | Page/runtime adapter lifetime; browser implementations exist only where behavior already existed outside native Tauri | **Renderer transport/platform adapters plus test-backend mirror.** Neutral interfaces have no implementation dependency; composition modules select or expose concrete adapters. | IPC names and Rust DTOs remain authoritative. Composition tests prove construction/selection; ports own stateless platform operations, not Matrix/product state. | **Phase 2 complete (2A and 2B1–2B4) as structural isolation; not semantic migration.** Every production Tauri import is adapter-owned and statically allowlisted. |
| Public `CoreRuntime` / `CoreConnection` command-event-snapshot-shutdown API | In-process Rust runtime and per-consumer connection lifetime | **Frontend-neutral Rust resource/state boundary.** Core owns command admission, events, snapshots, backpressure recovery and ordered shutdown; no Tauri type participates. | Integration test starts Core, submits connection-scoped `UpdateSettings`, observes event/version convergence, compares an independent consumer, and awaits shutdown; lag test resyncs then shuts down. | **Proven in Phase 6.** Tauri `FrontendDesktopSnapshot` remains adapter-only. |
| `CoreConnection::wait_for_request_outcome` and `runtime::request_outcome` | One attached Core connection per wait; lifetime of the caller's accepted request | **Core request-settlement service.** Versioned snapshots are authoritative; broadcast events are typed wake/payload signals. | Closed expectation variants require exact request/account/target/submission guards, one absolute deadline, an expectation-specific lag policy, and a final snapshot check after lag, closure, or timeout. A separate waiting connection may consume another connection's globally unique `RequestId`; it never uses its own connection ID as a filter. | **Phase A #755.** Adds no reducer transition. Existing Tauri waiters remain until later #755 phases. |
| Core `media_save` policy plus the Tauri `NativeMediaSaveFilesystem` port | One save command; native port exists for the adapter call | **Split Core policy/platform boundary.** Core owns source/destination validation, canonical cache/source containment, symlink and component-prefix rejection, parent-creation ordering, and copy admission. Tauri supplies roots and syscalls only. | Closed IO/policy kinds have private-safe `Debug`/`Display`; no path crosses Core state, events, commands, or diagnostics. | **Phase C #755.** No reducer transition; browser portability remains possible through another port. |
| Core `ThreadRootProjectionService` plus Room `TimelineActor::DisplayProjectionState` | Account/Room actor lifetime; dormant records survive bounded-window replacement and clear only from authoritative aggregate/redaction or room/session teardown | **Rust product/display lifecycle.** Core owns hydration, revisions, retained root snapshots, root/latest placement, stable display identity and explicit clear. Rust State mirrors typed transitions. | Display InitialItems/diffs are actor-generation-fenced and share one index domain; the 120-root admission bound includes dormant records; teardown cancels and awaits projection workers. | **Migrated in Phase 1 #708.** TypeScript has no projection map, bounded-window pruning, reply suppression, placeholder synthesis or placement decision; it maps Rust rows and retains renderer-only dividers/DOM behavior. |
| `TimelineView.tsx` mounted viewport controller (`pendingMeasuredHeightsRef`, anchors, range epochs, observers, frames) | Mounted timeline key; ordered teardown on key change/unmount | **Renderer presentation**. DOM measurement, virtualization, scroll anchors and visible-range facts have no backend owner. | ResizeObserver/frames/timers cancel at one key reset; Rust receives typed viewport facts. | **Keep.** #551 residual audit proves this cohesive DOM owner. |
| `TimelineView` Room DOM evidence and App Focused/Thread store evidence | Mounted renderer/key lifetime; safe to drop on key change or unmount | **Renderer presentation/diagnostics only.** Core owns focused projection commitment and gap relay/display commitment through `FocusedProjectionCommitted` and `GapProjectionRelayed`. | Renderer evidence has no DesktopApi command, retry/backoff owner, Core admission role, navigation consequence, or repair timeout. Exact actor/request/timeline/repair generations are fenced inside the Rust actor tree. | **#759 supersedes Phase 3 delivery.** Delete `timelineAcknowledgementDelivery.ts` and both projection/render ACK commands; keep only local layout evidence. |
| `TimelineView.tsx::pendingKeyRequests`, `keyRequestEpochRef`, `keyRequestToast` | Mounted key/account; reset on timeline-key change and Rust terminal DTO | **Renderer-specific immediate feedback/dispatch suppression**, not product admission. Rust owns `DecryptRetryController::admit`, `begin_decrypt_retry`, `handle_request_room_key`, and `TimelineActor.key_request_states`. | Duplicate click suppression, typed Rust pending/terminal clear, A→B→A rejection fence, keyboard/ARIA-live feedback and private fixed copy are covered. | **Keep, proven renderer-specific.** Rust cannot publish pending before the first command crosses transport; the Set/toast own only that gap and mounted feedback. |
| `TimelineView.tsx`/sidebar/member avatar visibility hooks, App snapshot-demand set and bounded demand registry, and `domain/avatarThumbnails.ts` | Mounted virtual/member window/key; snapshot and visible consumers release on disappearance, account replacement, or unmount | **Renderer visibility demand only.** React discovers visible/not-requested MXCs and submits typed demand. Core owns single-flight deduplication, bounded concurrency, the two-attempt network policy and session-terminal Ready/Failed cache. | The App registry reference-counts snapshot and visible consumers and retains only the opaque request sequence needed to cancel a released Core waiter. Visibility cleanup releases one consumer; Core removes only that waiter, aborts the last active/queued task, advances pending work, and rejects late completion. Neither layer classifies retryability or counts attempts. Core session generation fences account teardown and resets tasks/cache. | **Migrated in #761; cancellation completed in the umbrella continuation.** Retry classifiers/counts must not return to `src/`; revisit only if visibility demand itself moves behind a native viewport adapter. |
| `domain/desktopAttention.ts::createDesktopBadgeSoundDispatcher` | Page/platform-adapter lifetime; resets when sound policy/capability is unavailable | **Explicit renderer platform-mechanics exception.** Rust owns attention candidates/counts, sound capability and persisted user policy. The adapter owns only positive-edge detection, cooldown and one in-flight webview/window sound call. | Fixed three-second cooldown, generation fence and no Matrix identifiers/content; `desktopAttention.test.ts` covers first snapshot, positive edges, cooldown, failure and policy reset. | **Keep until a native Core-owned notification dispatcher replaces the sound port.** Must not expand into Matrix attention classification. |
| `TimelineView.tsx` backfill epochs/evaluation/ref fences | Mounted key; cancels with projection/layout reset | **Renderer presentation** for when geometry warrants asking. Rust owns pagination operation/end state and SDK task. | Prevents repeated DOM-triggered requests until layout/projection settles; no Matrix history semantics synthesized. | **Keep.** Revisit only with a whole viewport-controller redesign. |
| `latestTextMutationQueueRef` alias lanes (`alias:${userId}`) | Per-user autosave submission/result lifetime | **Renderer-specific bounded owner.** Rust owns durable aliases/Saving/reconciliation/projections; renderer serializes intent before pre-terminal Tauri/browser results. | Deferred first/latest autosave, stale-result suppression, rejected-tail continuation, bounded cleanup and direct-call source guard. | **Reconciled in Phase 5A.** Retain only as explicit transport/result sequencing; not product state. |
| `latestTextMutationQueueRef` main/thread caption lanes | Mounted-editor mutation lifetime keyed by target/root/staged id | **Renderer-specific bounded owner.** Rust owns staged caption DTO/residency/send; renderer orders editor intent through the correlated Tauri terminal and invalidates removed items. | Concrete main A/B/A, independent main/thread keys, rejection continuation, exact invalidation and App source funnels. | **Reconciled in Phase 5B.** Retain until Rust carries an editor revision. |
| `App.tsx::{room,space}Settings{Load,Request}Ref` | Page lifetime; demand marker resets on target navigation and same-target view replacement; request epoch advances for each local panel/profile intent | **Renderer presentation / transport fence.** Rust owns settings fetch/update semantics, correlated terminals and snapshot state; these refs own only whether a React panel intent remains current and whether its mount effect already dispatched. | Same-room equal-generation People results settle by latest renderer intent; load markers suppress duplicate effect dispatch, and only a still-current rejected request/target releases its marker for later retry. | **Keep; Phase 4.1 proven renderer-specific.** Rust cannot know panel/dialog lifetime, and appStore generation admission cannot order equal-generation same-room view intents. |
| `App.tsx::diagnosticsOpenIntentEpochRef` | Page/renderer lifetime; intentionally survives account replacement | **Renderer presentation.** Rust/Tauri own privacy-safe global/runtime diagnostic content; this epoch owns only which overlapping click may open the one dialog. Diagnostic snapshots are outside AppState/appStore and carry no request or state generation. | Both success and fixed-token failure settle only the latest open intent; stale completion cannot replace, annotate or reopen a later dialog. | **Keep; Phase 4.2 proven renderer-specific.** Moving dialog intent into Rust or serializing clicks would invert ownership/change behavior without a stronger backend fence. |
| `App.tsx::spaceMembersPanelOpenIntentEpochRef` and bounded `spaceMembersLoadDemandRef` | One current ready-account/Space/generation demand; record replaced on account/device/Space/generation change | **Renderer panel demand/resource.** Rust owns selection, generation, Loading request ID, SDK projection and persistent sync demand; React owns whether one panel open remains current and pre-projection duplicate suppression. | Full homeserver/user/device + Space/generation fence; one identity-safe in-flight/loaded record; old completion cannot apply, clear or mark a newer demand. | **Keep, bounded in Phase 4.3a.** Replaces unbounded account-less Map/Set; same-valued account replacement reloads. |
| `SpaceMembersPanel::inviteSearchRequestRef`, App `inviteWorkflowLifetimeEpochRef`, and Tauri open/search/close convergence wait | Mounted panel plus cross-surface workflow intent; Tauri task bounded to 2 seconds | **Split renderer/Rust transport boundary.** Rust owns invite query/candidates/scope/workflow; panel epoch owns local candidate list/spinner, App epoch owns room/Space surface lifetime, Tauri waits for exact Rust projection. | Exact destination/query/account fences; fixed timeout error; stale returned promise cannot directly setSnapshot/return candidates; StateDelta remains Rust-authoritative. | **Keep/migrated in Phase 4.3b.** Submit-then-immediate-snapshot race removed without new query semantics or IPC shape. |
| Space-member invite execution (`inviteUserToSpace`) | Rust Space-member operation lifetime | **Rust product authority.** App dispatches with full account/Space/generation fence but has no latest-click request epoch. | Rust first-admitted request wins; duplicate is correlated rejection; matching returned snapshot/appStore applies authoritative settlement. | **Migrated in Phase 4.3c.** Removed `spaceMembersInviteRequestRef`, which could suppress accepted success after rapid duplicate rejection. |
| Space invite cancellation + `spaceMembersCancelFailureEpochRef` | Rust cancellation operation plus renderer panel failure lifetime | **Split owner.** Rust first-admitted request owns membership settlement; navigation/full fence gates returned view, and the epoch gates only latest local transport-failure presentation. | Success ignores failure epoch, preserves resolved Rust Failed mapping, clears failure on non-failed settlement; catch requires latest failure epoch + target still invited. | **Migrated in Phase 4.3d.** Removed latest-click success authority; navigation clears stale failure. |
| Space-member role update + `spaceMembersRoleFailureEpochRef` | Rust role operation plus renderer-local failure lifetime | **Split owner.** Rust first-admitted request owns role settlement; Space navigation/full fences gate returned semantic state, and the epoch gates only local transport-failure presentation. | Duplicate both orders, same-Space navigation, panel close/reopen, current private rejection, resolved failure/retry/reload. | **Migrated in Phase 4.3e.** Latest-click success authority removed; confirmation/focus remain renderer-only. |
| `roomNavigationIntentEpochRef` / `spaceNavigationIntentEpochRef` | Pre-submit renderer view intent across async composer drain and promise follow-ups | **Renderer-specific owner.** Epochs prevent older local continuations after newer clicks; Rust owns submitted SelectRoom/SelectSpace commands and projections. | Rapid room A/B out-of-order; rapid Space A/B submits only latest after drain; cross-Home/Space/DM stale proofs. | **Reconciled in Phase 4.4.** Retained and renamed; no duplicate product state. |
| `App.tsx` search debounce timer and query drafts | Dialog/view lifetime | **Renderer presentation** (typing draft and debounce of user intent). Rust owns search request/result correlation and crawler. | Timer clears on query/view changes; no durable retry or result semantics. | **Keep.** |
| `App.tsx` `pendingRoomLeave`, leave/confirm/dialog state, widths, pointer listeners, focus timers | Overlay/gesture lifetime | **Renderer presentation**. Matrix membership and operation state stay Rust-owned. | Explicit cancel/unmount cleanup; in-flight guard prevents accidental repeated UI intent. | **Keep.** Accessibility basics remain frontend-owned. |
| `App.tsx` composer overlays + debounce handles + `typingSignalRef` | Renderer/key lifetime, released on account/target/revision transitions | **Renderer presentation** over Rust `ComposerDocument`, revision, store, lease and typed-intent authority. | IME-safe local draft overlay settles only against accepted Rust revision; typing ref dedupes renderer intent; timer/overlay teardown is renderer-local. | **Keep.** Do not move DOM/input buffering. |
| `App.tsx::composerDraftLifecycleRegistryRef` | Page renderer generation; leases acquired/released through typed backend | Shared resource boundary: frontend owns renderer handle; Core exclusively owns generation/lease allocation, Ready account/active target admission, scope, and permits. Canonical nonzero decimal wire strings are opaque transport only; Tauri has no identity registry. | Awaited acquire/release, canonical/invalid/zero/overflow tokens, generation replacement, forged/cross-scope rejection, and #657 harness mirror cleanup. | **Keep renderer lifecycle; Phase D #755 removed the adapter mirror.** One product authority remains in Core. |
| `App.tsx::submissionRegistryRef` and send overlays | Page/account/target; clears/settles from Rust submission IDs | Immediate presentation controller; Rust global submission registry/terminal state is authoritative. | Prevents local double UI settlement and preserves IME draft; terminal comes from Rust. | **Keep / audit only if duplicate transition is demonstrated.** |
| `App.tsx` ordered state/Core-event and Tauri menu listeners | Page/runtime transport lifetime | **Transport resource owner** in the renderer. | Each listener has cleanup. One versioned state-update lane applies deltas; initial/gap/lag/settlement resync snapshots are explicit. No second state-wake URI, debounce refresh timer, or command-returned snapshot path remains. | **Keep the listeners/cache only.** Backend task lifetime and product progress remain Rust-owned. |
| `App.tsx` QA send refs, diagnostics request generations, module error listeners | QA/page lifetime only | QA presentation/observability, not product state. | Reset by QA flow/page; privacy-safe diagnostics. Module error listeners overlap boot capture defensively. | **Keep; low-priority deletion audit** for duplicate error listeners, not a Rust migration. |
| `SessionVerificationGate` secure-backup confirmation dialog, retry in-flight ref, and button guards | Dialog/view lifetime; confirmation resets on gate or account owner change | **Renderer presentation** while Rust owns gate × typed-intent admission and SDK authority. Cancel emits no command; confirm sends `Reenable { confirmed: true }`; Tauri transports the enum only. | Accessible catalog-backed dialog, stale gate/account reset, cancel/no-command, exact confirm IPC, duplicate and stale intents rejected before actor routing. | **Phase E #755.** Keep dialog/input state only; no renderer or adapter confirmation policy. |
| `backend/browserFakeApi.ts` composer leases/draft maps/prepared bytes/submission ledger | Browser Fake instance/page | **Test-backend mirror** of Rust contracts. Not production state. | Bounded/reconciled by fake session/target generation; fixtures emulate terminal results. | **Keep as mirror; never cite as migration target.** Drift is test debt fixed against Rust. |
| `backend/browserFakeApi.ts` Activity/Space-member/search/settings local transitions | Browser Fake instance | **Test-backend mirror**, some duplicated state-machine logic intentionally required for browser tests. | Must install Rust-shaped snapshots and reproduce request/generation/failure guards. Merged #570/#582 behavior is part of the current Rust-shaped mirror contract. | **Keep and reduce duplication only in each reviewed contract migration.** |
| `apps/desktop/src/test/appHarnessMain.tsx::preparedUploadBytes`, `composerLeases`, invocation history | One Playwright harness page | **Test harness resource mirror**. #657 added snapshot/account/target reconciliation and boot history boundary. | Bytes and leases retire on authoritative replacement; invocation history has one boot boundary. | **Keep.** Already-correct reviewed lifecycle, not product state. |
| Pure dialogs, hover/focus/animation, alias drafts, media-viewer focus | Component mount/overlay | **Renderer presentation** | React cleanup and accessibility lifecycle only. | **Keep.** |

## Current migration-boundary evidence

- **Direct Tauri imports:** six production modules import `@tauri-apps/*`, all backend adapters: `backend/client.ts`, `backend/tauriTimelineTransport.ts`, and `backend/tauri/{desktopAttentionPort,desktopEventPort,linkMediaPort,windowDialogPort}.ts`. App, domain, hooks, composition roots, and neutral contracts are Tauri-free; ESLint and the App source contract enforce this exact allowlist.
- **Neutral API/port dependency:** `DesktopApi` lives in `backend/desktopApi.ts`; `TauriDesktopApi` and `BrowserFakeApi` implement it; `appRuntime` selects it. Link/media, attention, window/dialog and event operations use neutral contracts plus composition modules. `DesktopEventPort` unwraps only Tauri envelopes/channel names; App retains listener disposal, ordering, debounce, QA and menu semantics, while `tauriTimelineTransport` retains its readiness barrier and command adapter. All runtime checks import the leaf `backend/runtimeEnvironment.ts`. Phase 2 is structurally complete.
- **Mutation queue reconciliation:** Phase 5A proves local-alias lanes are bounded renderer-specific pre-terminal autosave sequencing; Phase 5B proves main/thread staged-caption lanes are bounded mounted-editor/terminal ordering. Invite and mention query keys were removed by merged PR #683.
- **Projection commitment:** TimelineView Room DOM evidence and App Focused/Thread store evidence are renderer-local only. `FocusedProjectionCommitted` and `GapProjectionRelayed` carry exact internal Core facts; no App-lifetime acknowledgement delivery controller or Tauri ACK command participates in navigation or repair.
- **App request-ref families:** Phases 4.1–4.3b classify/bound renderer demand; Phase 4.3c removes invite execution's latest-click ref; Phases 4.3d–4.3e narrow cancel/role epochs to local failure only while Rust owns settlement; Phase 4.4 proves and names room/Space pre-submit renderer intent epochs.

## Already-correct Rust-owned paths

The following are not migration work:

- Application/session/settings/invite/Activity/Space-member state in `koushi-state::AppState`; React consumes snapshots/deltas (`docs/agents/state-ownership.md`, “The boundary”).
- Composer persistence, revision, lease admission and send/submission terminals in Core/state. Frontend overlays are IME/render-local.
- Timeline SDK actors, pagination, repair, thread attention, read-state outbox, media tasks and room-key recovery in Core. `timelineStore` is a projection reducer, not the SDK owner.
- Room-key request admission/coalescing (`DecryptRetryController::admit`, `begin_decrypt_retry`, `handle_request_room_key`) and per-event projected request state (`TimelineActor.key_request_states`).
- Search crawler, directory, room/Space operations and current-session status. App request refs only fence stale renderer promises.
- Harness cleanup from #657, invite admission from #658, composer-load evidence from #645, KaTeX admission from #668, transient projection/trust-loss reset contracts from #660, and the later Rust-owned read-state, Activity/redaction, Space-role, authentication-invalidation, room-list session-fence, verification, active-session management, and login/store lifecycle changes merged through #702.

## Final duplicate-semantics reconciliation

1. **Thread-root lifecycle/placement:** migrated in Phase 1 #708; Core service/Room actor own lifecycle and placement, State mirrors typed transitions, and TypeScript only caches/renders Rust display items.
2. **Text mutation ordering:** invite/mention query authority migrated; Phases 5A–5B prove retained alias/caption lanes are bounded renderer transport/editor ordering around Rust-owned product state.
3. **Projection commitment:** #759 supersedes Phase 3's pre-Core ACK delivery. DOM/store evidence remains renderer-owned presentation only; reliable exact-generation internal Core signals own focused navigation and repair continuation.
4. **App request refs:** Phases 4.1–4.4 migrate latest-click semantic inversions and prove every retained settings/diagnostics/Space-member/navigation epoch is renderer view/feedback intent.
5. **Room-key request refs:** final audit classifies the Set/epoch/toast as mounted immediate feedback and pre-projection duplicate suppression; Rust owns admission/coalescing/terminal state.
6. **Browser Fake/harness transitions:** intentional bounded test mirrors, never production authorities; change only with their Rust contract.

No duplicate production semantic owner remains unresolved.

## Completed disjoint leaf record

### 1. Current Phase 1 result: Rust-owned thread-root projection lifecycle (#708)

- **Value:** fixes observed thread disappearance/reappearance by removing bounded-window deletion and placement authority from State/TypeScript.
- **Owner:** `ThreadRootProjectionService` plus current Room `TimelineActor::DisplayProjectionState`; TypeScript remains cache/render only.
- **Proof:** deterministic RED captures State/frontend bounded-window deletion; GREEN covers dormant retention, authoritative zero/redaction/unsubscribe clear, Rust-owned root/latest ordering, existing-thread initial pagination terminal policy, bounded awaited teardown, wire removal, renderer-only input, and replay/repair/event-order convergence.
- **Scope:** this reviewed #708 PR precedes adapter isolation and remains disjoint from later ACK/request-ref/mutation work.

### 2. Shipped: retire invite/mention query admission from the latest-text queue

- **Value:** removes the second “which async result wins” semantic owner from App for invite and mention queries while preserving the queue where it serializes unversioned mutations.
- **Proof required:** delayed invite and main/thread mention A/B/A dispatch, adversarial settlement, explicit monotone nonzero snapshot generations, rendered final projection, and account/room/dialog replacement fences. Alias/caption A/B/A serialization and invalidation stay green.
- **Scope:** `App.tsx`, `domain/latestAsyncResult.ts`, focused App/latestAsync/appStore tests, and this inventory/plan; no new dependency, Rust/Tauri API, fake semantic, or Rust abstraction.
- **Current result:** merged PR #683 migrated invite/mention query admission to Rust request/generation state plus `appStore`; Phases 5A–5B prove the remaining alias/caption mutation lanes renderer-specific and bounded.
- **Disjointness:** merged #659 changed room-list reducer admission; #608 authentication invalidation diagnostics/copy; #559 read-state local/server boundaries; and #570 Activity/redaction/thread convergence. None share this query leaf.

### 3. Superseded Phase 3: projection/repair ACK delivery owner

- **Historical result:** Phase 3 centralized four renderer ACK delivery channels in one bounded App adapter.
- **#759 replacement:** Core now settles focused projection and gap relay/display commitment through reliable internal generation-fenced signals. The adapter, its retry/backoff tests, and both DesktopApi/Tauri/Core ACK commands are deleted; renderer evidence remains local and safe to drop.

### 4. Shipped Phases 4.1–4.4: App request families

- **Value:** remove local stale-result fences already represented by Rust request IDs/generation and appStore admission.
- **Method:** one family per PR, exact delayed-result test, no generic request manager.
- **Phase 4.1 result:** room/Space settings refs are retained with explicit ownership proof; same-room equal-generation supersession, panel replacement, pending dedupe and rejection/retry checks show that they protect local view demand rather than duplicate Rust settings authority.
- **Phase 4.2 result:** diagnostics open intent is retained and named explicitly; overlapping success/failure, close/no-reopen and source-boundary checks prove that Rust owns diagnostic content while React owns one dialog's latest open intent.
- **Phase 4.3a result:** Space-member panel/open load demand remains renderer-owned but is reduced from unbounded account-less Map/Set bookkeeping to one account/Space/generation record; Rust retains projection and operation authority.
- **Phase 4.3b result:** room/Space invite-workflow returned promises share one renderer lifetime epoch, Space search keeps its mounted-list epoch, and Tauri open/search/close waits up to two seconds for exact Rust projections instead of racing current_snapshot.
- **Phase 4.3c result:** Space invite execution deletes the frontend latest-request epoch; deterministic rapid-duplicate settlement proves Rust's first-admitted request remains authoritative.
- **Phase 4.3d result:** cancellation success uses navigation/full fences and Rust authority; renamed failure epoch gates catch/UI only, including duplicate settlement and panel close/reopen proofs.
- **Phase 4.3e result:** role success uses navigation/full fences and Rust first-admitted authority; renamed failure epoch gates catch/UI only while correlated Rust failure/retry/reload remains intact.
- **Phase 4.4 result:** room/Space navigation counters are retained as explicitly named renderer intent epochs because intent precedes async composer drain and Rust command submission.

Low priority: consolidate duplicate QA error listeners after a behavioral boot-error proof. This is deletion, not Rust migration.

## Shipped changes since the prior inventory base

- #659 / PR #675: room-list session-fence acceptance is merged.
- #570 / PRs #676, #680 and #682: SDK aggregates plus Activity/thread/room-latest redaction/edit convergence are merged; #570 is closed.
- #582 / PR #677: direct Space-member role management is merged; #582 is closed.
- #608 / PR #681: UnknownToken authentication invalidation is separated from E2EE trust; #608 is closed.
- #552 / PR #683: invite/mention query ordering is migrated to existing Rust authority.
- #559 / PR #684: local-viewed and server-confirmed read-state convergence is merged; #559 is closed.
- #694/#699 work through PRs #695, #697 and #702 strengthens Rust-owned verification, active-session management, and login/store lifecycle without creating a new frontend semantic owner.

## Disjoint issue contracts

The following contracts are now shipped and remain disjoint evidence, not pending #552 work:

- #659: fail closed before late room-list readiness/invites/rooms/spaces mutation.
- #608: classify UnknownToken authentication invalidation separately from E2EE trust and update locked UI copy.
- #559: split local viewed boundary from server-confirmed read state and bound persistent retry/outbox behavior.
- #570: redacted/edit convergence in Activity/unread/thread/conversation projections.

The shipped invite/mention query leaf touches none of those owners; Phases 5A–5B document alias/caption lanes as separate bounded renderer transport/editor-result owners.

## #552 final acceptance status

| Epic criterion | Final evidence |
| --- | --- |
| Publish evidence-based inventory/classification | **Complete:** final inventory above is pinned to `ca9dc745` and classifies every long-lived site by authority/lifetime/settlement/decision. |
| Identify already-correct Rust-owned/projection-only paths | **Complete:** `appStore`, `timelineStore`, Core actors, SDK resources, renderer DOM owners and test mirrors are explicit. |
| Identify duplicated Rust/TS semantics | **Complete:** thread roots, query admission, projection commitment and Space-member latest-click inversions were migrated; retained request/mutation/key-request lanes have renderer-specific proofs. |
| Migrate selected high-value leaf owners incrementally | **Complete:** Phases 1–6 shipped one ownership seam per PR across thread roots, adapters, ACK delivery, request families, mutation sequencing and Core consumption. |
| One documented semantic owner per migrated subsystem | **Complete:** durable Matrix/product/resource authority is Rust; retained frontend owners are mounted intent/feedback, projection caches, adapters or bounded test mirrors. |
| Async Rust owners have cancellation/awaited settlement where required | **Complete:** #708 workers cancel/await on replacement/teardown; existing actors/tasks retain ordered shutdown; #759 internal projection/relay commitments are generation-fenced and renderer-independent; Phase 6 proves awaited Core shutdown. |
| Remove corresponding TS semantic state after cutover | **Complete:** thread lifecycle/placement maps, the projection/render ACK delivery adapter, invite latest-request ref and unbounded Space-member load maps are removed; cancel/role refs are failure-only. |
| Frontend cleanup primarily renderer-local | **Complete:** retained state is DOM/layout/focus/dialog/input/view intent, projection/cache, platform adapter or test mirror. |
| Preserve Tauri command/event compatibility unless separately reviewed | **Complete:** Phases 2–6 preserved their command/event/DTO names. #759 is the separate reviewed v1 state-update/command-settlement migration and lands atomically without compatibility shims. |
| Focused transition/teardown/projection-equivalence tests | **Complete:** deterministic frontend deferred tests, Rust reducer/runtime tests, Playwright DOM evidence, actor teardown and both real-server CI lanes cover each seam. |
| Compatible with Tauri UI and future native Rust renderer | **Complete:** React/Tauri remains shipping; Phase 6 consumes public Core start/connection/command/event/versioned snapshot/lag recovery/awaited shutdown without Tauri types. |

The inventory has no unresolved or merely “investigate” semantic owner. #552 is ready to close after the final audit PR passes exact-head CI and merges.
