# Issues 235, 238, and 239 Dogfood Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Root-fix issue #239, complete native attention issue #238, verify the existing issue #235 implementation, and merge one non-squash PR.

**Architecture:** Rust/Core owns Matrix state, relay lifecycle, submission acceptance, notification policy, and capabilities. Tauri is a typed transport/native adapter; React owns only ephemeral composition and in-flight UI guards. Canonical timeline data remains separate from the existing #235 display projection.

**Tech Stack:** Rust, Tokio, matrix-rust-sdk UI timeline, Tauri v2, React, TypeScript, Vitest, Playwright, local Conduit/Tuwunel QA.

---

### Task 1: Record the #239 relay lifecycle contract

**Files:**
- Modify: `docs/architecture/state-machine.md`
- Modify: `crates/koushi-core/src/timeline.rs`

- [ ] Add the generation-tagged overflow/resubscribe transitions and stale-batch guard to the timeline actor canon.
- [ ] Add a unit-test harness that fills the data inbox and proves the overflow control signal is delivered.
- [ ] Run `cargo test -p koushi-core --lib relay_overflow -- --nocapture` and record the expected pre-fix failure.
- [ ] Move relay task ownership into the actor, tag `DiffBatch` with its generation, and make overflow terminate the old relay through a lossless control path.
- [ ] Resubscribe for a matching snapshot and stream, emit `ResyncRequired` then `InitialItems`, start the new relay, and discard stale generations.
- [ ] Rerun the focused relay tests and commit with `fix(timeline): resubscribe relay after overflow`.

### Task 2: Prove authoritative echo convergence after relay recovery

**Files:**
- Modify: `crates/koushi-core/src/timeline.rs`
- Modify: `crates/koushi-core/tests/runtime_timeline.rs`
- Modify: `apps/desktop/src/domain/timelineStore.test.ts`

- [ ] Add separate RED coverage for a remote echo arriving before send completion without `unsigned.transaction_id`, followed by authoritative resync.
- [ ] Assert the final snapshot/store contains one event-ID row and no transaction row with `sending` state.
- [ ] Run the focused Core and timeline-store tests and confirm the new assertions fail before any convergence fix.
- [ ] Fix only the authoritative Rust snapshot/diff lifecycle needed for convergence; do not add body/time/sender dedupe or React row expiry.
- [ ] Rerun both focused suites and commit with `test(timeline): prove local echo convergence after resync`.

### Task 3: Give the IME one shared epoch lifecycle and DOM ownership

**Files:**
- Create: `apps/desktop/src/domain/compositionLifecycle.ts`
- Create: `apps/desktop/src/domain/compositionLifecycle.test.ts`
- Modify: `apps/desktop/src/components/composer.tsx`
- Modify: `apps/desktop/src/components/composer.test.tsx`

- [ ] Add a fake-timer RED test for `compositionEnd(A) -> compositionStart(B) -> A timer` that requires B to remain active.
- [ ] Add RED tests for main/thread/edit composers proving parent value updates during composition do not replace DOM value or selection.
- [ ] Run the two focused Vitest files and confirm the new tests fail for the current boolean/controlled implementation.
- [ ] Implement a shared epoch helper that cancels stale deferred ends and exposes a unified composing predicate including epoch state, native `isComposing`, and key code 229.
- [ ] Keep textarea DOM ownership stable during composition and synchronize external values imperatively only outside composition.
- [ ] Rerun the focused tests and commit with `fix(composer): preserve live conversion composition ownership`.

### Task 4: Make send acceptance idempotent across every layer

**Files:**
- Modify: `docs/architecture/state-machine.md`
- Modify: `crates/koushi-state/src/state/timeline.rs`
- Modify: `crates/koushi-state/src/reducer/timeline.rs`
- Modify: `crates/koushi-state/tests/composer_semantics_state.rs`
- Modify: `crates/koushi-core/src/command.rs`
- Modify: `crates/koushi-core/src/timeline.rs`
- Modify: `apps/desktop/src-tauri/src/commands/timeline.rs`
- Modify: `apps/desktop/src/backend/client.ts`
- Modify: `apps/desktop/src/App.tsx`
- Modify: `apps/desktop/src/components/composer.tsx`
- Modify: corresponding Rust, Tauri, IPC-contract, App, and composer tests

- [ ] Specify submission ID, duplicate handling, accepted/rejected outcomes, guard release, and draft-clear transitions in the state-machine canon.
- [ ] Add RED reducer/Core tests that submit one ID twice and require exactly one accepted transaction.
- [ ] Add a RED Tauri test proving `send_text`/reply cannot resolve before matching accepted/rejected state.
- [ ] Add a RED composer/App test with a delayed resolver and repeated Enter requiring exactly one invoke and no pre-acceptance draft clear.
- [ ] Carry one submission ID frontend-to-reducer, return a typed result after matching acceptance/rejection, and keep Retry on the existing transaction.
- [ ] Release guards only on rejection or accepted terminal lifecycle, clear draft only on acceptance, and add private-data-safe fixed outcome diagnostics.
- [ ] Run focused state, Core, Tauri, IPC-contract, and frontend tests; commit with `fix(send): enforce exactly-once submission acceptance`.

### Task 5: Complete notification sound, badge capability, and diagnostics

**Files:**
- Modify: `crates/koushi-state/src/state/native_attention.rs`
- Modify: `crates/koushi-state/src/reducer/native_attention.rs`
- Modify: `crates/koushi-state/tests/attention_surface.rs`
- Modify: `apps/desktop/src/domain/desktopAttention.ts`
- Modify: `apps/desktop/src/domain/desktopAttention.test.ts`
- Modify: `apps/desktop/src-tauri/src/lib.rs`
- Add or modify: platform-native attention adapter and bundled sound asset metadata

- [ ] Add RED tests for burst cooldown, disabled sound, unavailable/unknown badge capability, zero/logout clearing, and fixed adapter-failure diagnostics.
- [ ] Run the focused Rust and Vitest attention suites and confirm the added behavior fails.
- [ ] Implement deterministic candidate-only sound coalescing and a real Tauri/native sound adapter using a repository-compatible bundled asset.
- [ ] Gate numeric badges on Rust-owned platform capability, clear supported surfaces at zero/logout, and emit only fixed diagnostic outcomes on adapter failure.
- [ ] Add at least one adapter integration test, rerun focused tests, and commit with `feat(notifications): complete sound and native badge feedback`.

### Task 6: Verify #235 and cross-feature regressions

**Files:**
- Modify only if a regression is reproduced: existing #235 projection, viewport, or test files

- [ ] Run `cargo test -p koushi-state --test settings_state timeline_thread_root_order` and `cargo test -p koushi-state --test threads_list_state`.
- [ ] Run `npm --prefix apps/desktop test -- --run src/domain/timelineDisplayProjection.test.ts src/components/TimelineView.test.tsx src/components/UserSettingsPanel.test.tsx`.
- [ ] Run the #235 Playwright projection test with one worker and verify viewport/navigation behavior.
- [ ] If a regression fails, first preserve the failure as a focused RED test, make the minimal root fix, rerun, and commit separately.

### Task 7: Final verification, review, PR, and merge

**Files:**
- Modify: PR metadata and GitHub issue state only after all gates pass

- [ ] Run formatting, lint, typecheck, IPC contract, focused Rust/Tauri/frontend tests, and the applicable local headless scenarios.
- [ ] Obtain macOS Japanese live-conversion evidence; do not mark #239 complete from Linux-only evidence.
- [ ] Review the complete diff against all three issues, repository rules, architecture, privacy requirements, and prohibited #239 workarounds.
- [ ] Push `codex/issues-235-238-239-luna`, open one PR with `Closes #235`, `Closes #238`, and `Closes #239` only when their acceptance evidence is complete.
- [ ] Wait for required checks, repair failures through RED/GREEN, and merge with `gh pr merge --merge --delete-branch`.
